require 'spec_helper'

describe StoryTime do
  it 'has a version number' do
    expect(StoryTime::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
